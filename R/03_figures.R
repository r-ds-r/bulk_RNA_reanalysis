# =================================================================
# Custom settings for plotting functions
# =================================================================


# Define custom colors for conditions

annotation_colors <- list(
  condition = c(sham = "#2B5B3F", SNL = "#C4501B", SNI = "#C4501B")
)

# Define high definition figure size

FIG_WIDTH  <- 3000
FIG_HEIGHT <- 2400
FIG_RES    <- 300
FIG_UNITS  <- "px"
